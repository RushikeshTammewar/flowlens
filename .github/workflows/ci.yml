name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: https://api.flowlens.in

  backend:
    name: Backend
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            agent/requirements.txt

      - name: Install dependencies
        run: pip install -r backend/requirements.txt -r agent/requirements.txt

      - name: Syntax check
        run: |
          python -c "
          import ast, pathlib, sys
          failed = False
          for f in sorted(pathlib.Path('.').rglob('*.py')):
              if '.venv' in str(f) or 'node_modules' in str(f):
                  continue
              try:
                  ast.parse(f.read_text())
              except SyntaxError as e:
                  print(f'FAIL: {f} - {e}')
                  failed = True
          if failed:
              sys.exit(1)
          print('All Python files parsed OK')
          "

      - name: Import check (v2 — Browser-Use powered)
        run: |
          python -c "
          from agent.models.graph import SiteGraph
          from agent.models.types import BugFinding, CrawlResult
          from agent.models.flow import Flow, FlowStep, FlowResult
          from agent.core.navigation_engine import NavigationEngine, PageState, NavigationResult
          from agent.core.ai_engine import GeminiEngine
          from agent.core.qa_agent import QAAgent
          from agent.core.scanner import FlowLensScanner
          from agent.core.report import print_report
          from agent.detectors.functional import FunctionalDetector
          from agent.detectors.performance import PerformanceDetector
          from agent.detectors.responsive import ResponsiveDetector
          from backend.app.main import app
          print('All imports OK')
          "
