# Flowlens v3 root env example
# This file is for documentation only — actual env vars live in
# - apps/web/.env.local (server-side)
# - apps/extension/.env (build-time)
# - Vercel project settings (production)

# Existing v2 env vars (Python agent) — kept for legacy compatibility
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
